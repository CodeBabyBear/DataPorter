package com.newegg.eims.DataPorter

/**
  * Base is the DataPorter's core, it include lazy data set and struct data set,
  * and make data set like the scala collection
  */
package object Base {

  object ScalaReflectionLock
}
